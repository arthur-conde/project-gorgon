name: Shared MCP Servers
version: 0.0.1
schema: v1
mcpServers:
  - name: GitHub MCP
    command: npx
    args:
      - mcp-remote
      - https://api.githubcopilot.com/mcp/
      - --header
      - "Authorization: Bearer ${{ secrets.GITHUB_MCP_PAT }}"