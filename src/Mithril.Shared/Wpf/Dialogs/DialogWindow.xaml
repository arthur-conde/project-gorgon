<Window x:Class="Mithril.Shared.Wpf.Dialogs.DialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Mithril.Shared;component/Wpf/Resources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="#FF1A1A1A" BorderBrush="#33FFFFFF" BorderThickness="1" CornerRadius="6"
            MinWidth="360" MaxWidth="560">
        <DockPanel>
            <!-- Title bar -->
            <Border DockPanel.Dock="Top" Background="#FF202020" CornerRadius="6,6,0,0"
                    Padding="14,10" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <DockPanel>
                    <Button DockPanel.Dock="Right" Width="24" Height="24" Padding="0"
                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                            ToolTip="Close" Click="CloseButton_Click">
                        <icon:PackIconLucide Kind="X" Width="14" Height="14" Foreground="#88FFFFFF"/>
                    </Button>
                    <TextBlock x:Name="TitleText" FontFamily="{DynamicResource AppHeaderFontFamily}" FontSize="{DynamicResource AppFontSizeXLarge}"
                               Foreground="#FFD4A847" VerticalAlignment="Center"/>
                </DockPanel>
            </Border>

            <!-- Button row -->
            <Border DockPanel.Dock="Bottom" Padding="14,10" Background="#FF1E1E1E"
                    CornerRadius="0,0,6,6" BorderBrush="#33FFFFFF" BorderThickness="0,1,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="SecondaryButton" Padding="16,6" Margin="0,0,8,0"
                            Click="SecondaryButton_Click"/>
                    <Button x:Name="PrimaryButton" Padding="16,6"
                            Background="#FFD4A847" Foreground="#FF1A1A1A" FontWeight="SemiBold"
                            Click="PrimaryButton_Click"/>
                </StackPanel>
            </Border>

            <!-- Content area -->
            <Border Padding="14,12">
                <ContentPresenter x:Name="DialogContent"/>
            </Border>
        </DockPanel>
    </Border>
</Window>
