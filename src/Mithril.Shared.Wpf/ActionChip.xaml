<UserControl x:Class="Mithril.Shared.Wpf.ActionChip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:c="clr-namespace:Mithril.Shared.Wpf"
             xmlns:local="clr-namespace:Mithril.Shared.Wpf"
             xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Name="ChipRoot">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Mithril.Shared.Wpf;component/Converters.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <!-- Ghost / outline variant of EntityChip used for "open this filtered tab" shortcuts
         on detail panes. Visually subordinated so the user can tell action affordances
         apart from entity-data chips at a glance: transparent fill, subdued border,
         leading ListFilter icon. Same DataContext shape (EntityChipVm) and ClickCommand
         contract as EntityChip — drop-in replacement where the chip semantically means
         "navigate to a filtered view" rather than "open this specific entity". -->
    <Button Padding="6,2" Margin="0,1,4,1"
            Click="OnChipClicked"
            HorizontalAlignment="Left">
        <Button.Style>
            <Style TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="#33FFFFFF"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Foreground" Value="#66FFFFFF"/>
                <Setter Property="Cursor" Value="Arrow"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsNavigable}" Value="True">
                        <Setter Property="BorderBrush" Value="#66D4A847"/>
                        <Setter Property="Foreground" Value="#FFD4A847"/>
                        <Setter Property="Cursor" Value="Hand"/>
                    </DataTrigger>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#22D4A847"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Button.Style>
        <Button.Template>
            <ControlTemplate TargetType="Button">
                <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="3" Padding="{TemplateBinding Padding}">
                    <StackPanel Orientation="Horizontal">
                        <icon:PackIconLucide Kind="ListFilter" Width="12" Height="12"
                                             Foreground="{TemplateBinding Foreground}"
                                             Margin="0,0,4,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding DisplayName}"
                                   Foreground="{TemplateBinding Foreground}"
                                   FontSize="{DynamicResource AppFontSizeHint}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </ControlTemplate>
        </Button.Template>
    </Button>
</UserControl>
