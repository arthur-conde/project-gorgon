<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=System.Runtime"
                    xmlns:c="clr-namespace:Mithril.Shared.Wpf"
                    xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Mithril.Shared.Wpf;component/Converters.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- Shared row template for an EffectLine (icon + rendered text).
         Bind any IEnumerable<EffectLine> to an ItemsControl and set
         ItemTemplate="{StaticResource EffectLineTemplate}". Icon is hidden
         when IconId == 0 (prose rows and icon-less attributes).
         Layout: DockPanel (not StackPanel.Horizontal) so the TextBlock has a
         constrained width and TextWrapping="Wrap" actually wraps long descs. -->
    <DataTemplate x:Key="EffectLineTemplate">
        <DockPanel Margin="0,1" LastChildFill="True">
            <c:IconImage DockPanel.Dock="Left" IconId="{Binding IconId}" Width="16" Height="16" Margin="0,0,4,0"
                         VerticalAlignment="Top"
                         Visibility="{Binding IconId, Converter={StaticResource PositiveIntToVis}}"/>
            <TextBlock Text="{Binding Text}" Foreground="{DynamicResource TextSecondaryBrush}"
                       FontSize="{DynamicResource AppFontSizeHint}" TextWrapping="Wrap"/>
        </DockPanel>
    </DataTemplate>

    <!-- Font resources — overridden at runtime from ShellSettings (UiFontFamily, UiFontSize).
         Defaults here match the original hand-authored look so XAML designers render sensibly
         without the shell. All size keys scale proportionally from AppFontSize. -->
    <FontFamily x:Key="AppFontFamily">Segoe UI</FontFamily>
    <FontFamily x:Key="AppHeaderFontFamily">Cambria</FontFamily>
    <FontFamily x:Key="AppMonoFontFamily">Consolas</FontFamily>
    <sys:Double x:Key="AppFontSize">12</sys:Double>
    <sys:Double x:Key="AppFontSizeXSmall">9</sys:Double>
    <sys:Double x:Key="AppFontSizeSmall">10</sys:Double>
    <sys:Double x:Key="AppFontSizeHint">11</sys:Double>
    <sys:Double x:Key="AppFontSizeMedium">13</sys:Double>
    <sys:Double x:Key="AppFontSizeLarge">14</sys:Double>
    <sys:Double x:Key="AppFontSizeXLarge">16</sys:Double>
    <sys:Double x:Key="AppFontSizeHeader">18</sys:Double>
    <sys:Double x:Key="AppFontSizeTitle">20</sys:Double>
    <sys:Double x:Key="AppFontSizeHero">22</sys:Double>

    <!-- State opacities for cards / list items.
         Eaten = "done, de-emphasised"; Locked = "out of reach, even more de-emphasised". -->
    <sys:Double x:Key="EatenOpacityKey">0.6</sys:Double>
    <sys:Double x:Key="LockedOpacityKey">0.45</sys:Double>

    <!-- Palette — matches hand-authored colors in Samwise/Shell/Legolas views -->
    <SolidColorBrush x:Key="BgPrimaryBrush"      Color="#FF1A1A1A"/>
    <SolidColorBrush x:Key="BgRecessedBrush"     Color="#FF1E1E1E"/>
    <SolidColorBrush x:Key="BgSurfaceBrush"      Color="#FF252525"/>
    <SolidColorBrush x:Key="BgSurfaceHoverBrush" Color="#FF303030"/>
    <SolidColorBrush x:Key="TextPrimaryBrush"    Color="#FFE8E8E8"/>
    <SolidColorBrush x:Key="TextSecondaryBrush"  Color="#CCFFFFFF"/>
    <SolidColorBrush x:Key="TextTertiaryBrush"   Color="#88FFFFFF"/>
    <SolidColorBrush x:Key="AccentBrush"         Color="#FFD4A847"/>
    <SolidColorBrush x:Key="AccentSoftBrush"     Color="#FF1E3A5F"/>
    <SolidColorBrush x:Key="AccentSoftTextBrush" Color="#FFCFE8FF"/>
    <SolidColorBrush x:Key="BorderSubtleBrush"   Color="#33FFFFFF"/>
    <SolidColorBrush x:Key="BorderStrongBrush"   Color="#55FFFFFF"/>

    <!-- ===== Silmarillion #404 visual-grammar tokens (Phase 4) =====
         Design token to resource key reconciliation ledger for the G3 ratified
         grammar (docs/silmarillion-visual-grammar.md). Keys that already
         existed are REUSED (no duplication); only genuinely missing tokens are
         added here. Per the Phase 4 dispatch the design tokens are the source
         of truth and the WPF set is reconciled to them.

           token accent  D4A847        => AccentBrush         (exists; Fact-title, Link name)
           token fg.primary            => TextPrimaryBrush    (exists; Fact values)
           token fg.secondary          => TextSecondaryBrush  (exists)
           token fg.tertiary           => TextTertiaryBrush   (exists; Structure label, footer KEY/ROW value)
           token fg.quaternary         => TextQuaternaryBrush (ADDED)
           token bg.surface            => BgSurfaceBrush      (exists; Control chassis only)
           token bg.surface.hover      => BgSurfaceHoverBrush (exists)
           token border.subtle         => BorderSubtleBrush   (exists)
           token border.strong         => BorderStrongBrush   (exists)
           token border.faint          => BorderFaintBrush    (ADDED; footer divider)
           token accent.soft 1E3A5F    => AccentSoftBrush     (exists; SetRef active fill)
           token radius.sm (2)         => GrammarRadiusSm     (ADDED; SetRef)
           token radius.md (3)         => GrammarRadiusMd     (ADDED; general)

         FLAGGED RECONCILIATION (designer confirm, see Phase 4 carry-forward):
         the grammar specifies Set-reference text as token info #88B0E0,
         explicitly "matching q.keyword from MithrilQueryBox". The repo's actual
         query keyword brush is AccentSoftTextBrush / QueryKeywordBrush =
         #FFCFE8FF, not #88B0E0. The literal hex was the designer's
         approximation of a token they did not have; the ratified intent is
         pigment kinship with the query keyword colour. Honoring intent over the
         approximated literal, SetRefTextBrush is reconciled to the real query
         keyword value (#FFCFE8FF). One resource key, trivially re-tuned if the
         designer wants the literal #88B0E0. Surfaced here and in the PR, not a
         silent override. ===== -->
    <SolidColorBrush x:Key="TextQuaternaryBrush" Color="#66FFFFFF"/>
    <SolidColorBrush x:Key="BorderFaintBrush"    Color="#22FFFFFF"/>
    <SolidColorBrush x:Key="SetRefTextBrush"     Color="#FFCFE8FF"/>
    <SolidColorBrush x:Key="SetRefIdleFillBrush" Color="#4D1E3A5F"/>
    <SolidColorBrush x:Key="SetRefBorderBrush"   Color="#553398FF"/>
    <CornerRadius x:Key="GrammarRadiusSm">2</CornerRadius>
    <CornerRadius x:Key="GrammarRadiusMd">3</CornerRadius>

    <!-- Implicit styles — inline Foreground/Background attributes in existing views
         override these, so Samwise, the shell views and the Legolas overlays all
         keep their current hand-authored look. Only unstyled markup picks this up. -->

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}"/>
        <Setter Property="FontSize"   Value="{DynamicResource AppFontSize}"/>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Background"     Value="{StaticResource BgSurfaceBrush}"/>
        <Setter Property="Foreground"     Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush"    Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="CaretBrush"     Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource AccentBrush}"/>
        <Setter Property="Padding"        Value="4,2"/>
        <Setter Property="FontFamily"     Value="{DynamicResource AppFontFamily}"/>
        <Setter Property="FontSize"       Value="{DynamicResource AppFontSize}"/>
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}"/>
        <Setter Property="FontSize"   Value="{DynamicResource AppFontSize}"/>
    </Style>

    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}"/>
        <Setter Property="FontSize"   Value="{DynamicResource AppFontSize}"/>
    </Style>

    <Style TargetType="GroupBox">
        <Setter Property="Foreground"  Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="FontFamily"  Value="{DynamicResource AppFontFamily}"/>
        <Setter Property="FontSize"    Value="{DynamicResource AppFontSize}"/>
    </Style>

    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}"/>
        <Setter Property="FontSize"   Value="{DynamicResource AppFontSize}"/>
    </Style>

    <Style TargetType="Button">
        <Setter Property="Background"  Value="{StaticResource BgSurfaceBrush}"/>
        <Setter Property="Foreground"  Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="Padding"     Value="10,4"/>
        <Setter Property="MinHeight"   Value="24"/>
        <Setter Property="FontFamily"  Value="{DynamicResource AppFontFamily}"/>
        <Setter Property="FontSize"    Value="{DynamicResource AppFontSize}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="3"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource BgSurfaceHoverBrush}"/>
                            <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource BorderStrongBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource AccentSoftBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ToggleButton">
        <Setter Property="Background"  Value="{StaticResource BgSurfaceBrush}"/>
        <Setter Property="Foreground"  Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="Padding"     Value="10,4"/>
        <Setter Property="MinHeight"   Value="24"/>
        <Setter Property="FontFamily"  Value="{DynamicResource AppFontFamily}"/>
        <Setter Property="FontSize"    Value="{DynamicResource AppFontSize}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="3"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource BgSurfaceHoverBrush}"/>
                            <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource BorderStrongBrush}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource AccentSoftBrush}"/>
                            <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            <Setter Property="Foreground"  Value="{StaticResource AccentSoftTextBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource AccentSoftBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBox toggle-button sub-template (the clickable area + arrow glyph) -->
    <ControlTemplate x:Key="DarkComboBoxToggleButton" TargetType="ToggleButton">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="20"/>
            </Grid.ColumnDefinitions>
            <Border x:Name="Border" Grid.ColumnSpan="2" CornerRadius="3"
                    Background="{StaticResource BgSurfaceBrush}"
                    BorderBrush="{StaticResource BorderSubtleBrush}"
                    BorderThickness="1"/>
            <Border Grid.Column="0" CornerRadius="3,0,0,3" Margin="1"
                    Background="Transparent"/>
            <Path x:Name="Arrow" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                  Data="M 0 0 L 4 4 L 8 0 Z" Fill="{StaticResource TextSecondaryBrush}"/>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Border" Property="Background" Value="{StaticResource BgSurfaceHoverBrush}"/>
                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource BorderStrongBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.5"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- Editable text area inside an IsEditable ComboBox -->
    <ControlTemplate x:Key="DarkComboBoxTextBox" TargetType="TextBox">
        <Border x:Name="PART_ContentHost" Focusable="False" Background="Transparent"/>
    </ControlTemplate>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Background"  Value="{StaticResource BgSurfaceBrush}"/>
        <Setter Property="Foreground"  Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="Padding"     Value="6,4"/>
        <Setter Property="FontFamily"  Value="{DynamicResource AppFontFamily}"/>
        <Setter Property="FontSize"    Value="{DynamicResource AppFontSize}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgSurfaceHoverBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Foreground"   Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="Background"   Value="{StaticResource BgSurfaceBrush}"/>
        <Setter Property="BorderBrush"  Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="Padding"      Value="6,3"/>
        <Setter Property="MinHeight"    Value="24"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="FontFamily"   Value="{DynamicResource AppFontFamily}"/>
        <Setter Property="FontSize"     Value="{DynamicResource AppFontSize}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton"
                                      Template="{StaticResource DarkComboBoxToggleButton}"
                                      Focusable="False"
                                      ClickMode="Press"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                        <ContentPresenter x:Name="ContentSite"
                                          IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                          Margin="6,3,24,3"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"/>
                        <TextBox x:Name="PART_EditableTextBox"
                                 Style="{x:Null}"
                                 Template="{StaticResource DarkComboBoxTextBox}"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Center"
                                 Margin="4,3,24,3"
                                 Focusable="True"
                                 Background="Transparent"
                                 Foreground="{StaticResource TextPrimaryBrush}"
                                 CaretBrush="{StaticResource TextPrimaryBrush}"
                                 Visibility="Hidden"
                                 IsReadOnly="{TemplateBinding IsReadOnly}"/>
                        <Popup x:Name="Popup" Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" Focusable="False"
                               PopupAnimation="Slide">
                            <Grid x:Name="DropDown"
                                  SnapsToDevicePixels="True"
                                  MinWidth="{TemplateBinding ActualWidth}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border x:Name="DropDownBorder"
                                        Background="{StaticResource BgSurfaceBrush}"
                                        BorderBrush="{StaticResource BorderStrongBrush}"
                                        BorderThickness="1" CornerRadius="0,0,3,3"/>
                                <ScrollViewer Margin="1" SnapsToDevicePixels="True">
                                    <StackPanel IsItemsHost="True"
                                                KeyboardNavigation.DirectionalNavigation="Contained"/>
                                </ScrollViewer>
                            </Grid>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                        </Trigger>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter Property="IsTabStop" Value="False"/>
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══════════════════════ DataGrid ═══════════════════════ -->

    <!-- Thumb style for column header resize grippers -->
    <Style x:Key="DataGridHeaderGripperStyle" TargetType="Thumb">
        <Setter Property="Width" Value="6"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Cursor" Value="SizeWE"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background"  Value="{StaticResource BgSurfaceBrush}"/>
        <Setter Property="Foreground"  Value="{StaticResource AccentBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="FontWeight"  Value="SemiBold"/>
        <Setter Property="FontSize"    Value="{DynamicResource AppFontSizeHint}"/>
        <Setter Property="FontFamily"  Value="{DynamicResource AppFontFamily}"/>
        <Setter Property="MinHeight"   Value="28"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridColumnHeader">
                    <Grid>
                        <Border x:Name="HeaderBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="0,0,1,1"
                                Padding="6,4">
                            <DockPanel>
                                <Path x:Name="SortArrow" DockPanel.Dock="Right"
                                      Margin="4,0,0,0" VerticalAlignment="Center"
                                      Data="M 0 0 L 4 4 L 8 0 Z"
                                      Fill="{StaticResource TextSecondaryBrush}"
                                      Visibility="Collapsed"
                                      RenderTransformOrigin="0.5,0.5"/>
                                <ContentPresenter VerticalAlignment="Center"
                                                  RecognizesAccessKey="True"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </DockPanel>
                        </Border>
                        <Thumb x:Name="PART_LeftHeaderGripper"
                               HorizontalAlignment="Left"
                               Style="{StaticResource DataGridHeaderGripperStyle}"/>
                        <Thumb x:Name="PART_RightHeaderGripper"
                               HorizontalAlignment="Right"
                               Style="{StaticResource DataGridHeaderGripperStyle}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="SortDirection" Value="Ascending">
                            <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="SortArrow" Property="RenderTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="180"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="SortDirection" Value="Descending">
                            <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HeaderBorder" Property="Background" Value="{StaticResource BgSurfaceHoverBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="DataGridRow">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource BgSurfaceHoverBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource AccentSoftBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource AccentSoftTextBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="Padding" Value="4,2"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Foreground" Value="{StaticResource AccentSoftTextBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="DataGrid">
        <Setter Property="Background"              Value="#FF1E1E1E"/>
        <Setter Property="Foreground"               Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush"              Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="BorderThickness"          Value="1"/>
        <Setter Property="RowBackground"            Value="#FF1E1E1E"/>
        <Setter Property="AlternatingRowBackground" Value="{StaticResource BgSurfaceBrush}"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="GridLinesVisibility"      Value="Horizontal"/>
        <Setter Property="HeadersVisibility"        Value="Column"/>
        <Setter Property="AutoGenerateColumns"      Value="False"/>
        <Setter Property="IsReadOnly"               Value="True"/>
        <Setter Property="SelectionMode"            Value="Single"/>
        <Setter Property="CanUserSortColumns"       Value="True"/>
        <Setter Property="CanUserReorderColumns"    Value="True"/>
        <Setter Property="CanUserResizeColumns"     Value="True"/>
        <Setter Property="RowHeaderWidth"           Value="0"/>
        <Setter Property="FontSize"                 Value="{DynamicResource AppFontSize}"/>
        <Setter Property="FontFamily"               Value="{DynamicResource AppFontFamily}"/>
    </Style>

    <!-- ═══════════════════ MithrilDataGrid ═══════════════════ -->

    <!-- Column header variant whose PART_FilterBox visibility is gated by the
         parent MithrilDataGrid's FilterMode flag. Used only by MithrilDataGrid;
         the existing DataGridColumnHeader style above keeps the always-on filter
         row for legacy DataGrid consumers. -->
    <!-- Single-row column header for MithrilDataGrid — no per-column filter box.
         The grid's sole filter surface is the companion MithrilQueryBox. -->
    <Style x:Key="MithrilDataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
        <Setter Property="Background"  Value="{StaticResource BgSurfaceBrush}"/>
        <Setter Property="Foreground"  Value="{StaticResource AccentBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="FontWeight"  Value="SemiBold"/>
        <Setter Property="FontSize"    Value="{DynamicResource AppFontSizeHint}"/>
        <Setter Property="FontFamily"  Value="{DynamicResource AppFontFamily}"/>
        <Setter Property="MinHeight"   Value="28"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridColumnHeader">
                    <Grid>
                        <Border x:Name="HeaderBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="0,0,1,1"
                                Padding="6,4">
                            <DockPanel>
                                <Path x:Name="SortArrow" DockPanel.Dock="Right"
                                      Margin="4,0,0,0" VerticalAlignment="Center"
                                      Data="M 0 0 L 4 4 L 8 0 Z"
                                      Fill="{StaticResource TextSecondaryBrush}"
                                      Visibility="Collapsed"
                                      RenderTransformOrigin="0.5,0.5"/>
                                <ContentPresenter VerticalAlignment="Center"
                                                  RecognizesAccessKey="True"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </DockPanel>
                        </Border>
                        <Thumb x:Name="PART_LeftHeaderGripper"
                               HorizontalAlignment="Left"
                               Style="{StaticResource DataGridHeaderGripperStyle}"/>
                        <Thumb x:Name="PART_RightHeaderGripper"
                               HorizontalAlignment="Right"
                               Style="{StaticResource DataGridHeaderGripperStyle}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="SortDirection" Value="Ascending">
                            <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="SortArrow" Property="RenderTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="180"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="SortDirection" Value="Descending">
                            <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HeaderBorder" Property="Background" Value="{StaticResource BgSurfaceHoverBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Row style: extra padding for ReadOnly mode comfort. -->
    <Style x:Key="MithrilDataGridRowStyle" TargetType="DataGridRow">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource BgSurfaceHoverBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource AccentSoftBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource AccentSoftTextBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Cell style tweaks per Mode — ReadOnly uses tighter look, Editable shows focus ring. -->
    <Style x:Key="MithrilDataGridCellStyle" TargetType="DataGridCell">
        <Setter Property="Padding" Value="6,5"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border x:Name="CellBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="Transparent"
                            BorderThickness="1"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsKeyboardFocusWithin, RelativeSource={RelativeSource Self}}" Value="True"/>
                                <Condition Binding="{Binding Mode, RelativeSource={RelativeSource AncestorType={x:Type c:MithrilDataGrid}}}" Value="Editable"/>
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="CellBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                        </MultiDataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Foreground" Value="{StaticResource AccentSoftTextBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type c:MithrilDataGrid}" BasedOn="{StaticResource {x:Type DataGrid}}">
        <!-- Alt-row contrast bumped so rows read as distinct without grid lines. -->
        <Setter Property="RowBackground"            Value="#FF1E1E1E"/>
        <Setter Property="AlternatingRowBackground" Value="#FF262626"/>
        <Setter Property="ColumnHeaderStyle"        Value="{StaticResource MithrilDataGridColumnHeaderStyle}"/>
        <Setter Property="CellStyle"                Value="{StaticResource MithrilDataGridCellStyle}"/>
        <Setter Property="RowStyle"                 Value="{StaticResource MithrilDataGridRowStyle}"/>
        <Style.Triggers>
            <Trigger Property="Mode" Value="Editable">
                <Setter Property="IsReadOnly" Value="False"/>
                <Setter Property="CanUserAddRows" Value="False"/>
                <Setter Property="CanUserDeleteRows" Value="False"/>
            </Trigger>
            <Trigger Property="Mode" Value="ReadOnly">
                <Setter Property="IsReadOnly" Value="True"/>
                <Setter Property="CanUserAddRows" Value="False"/>
                <Setter Property="CanUserDeleteRows" Value="False"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="ToolTip">
        <Setter Property="Background"  Value="{StaticResource BgSurfaceBrush}"/>
        <Setter Property="Foreground"  Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="Padding"     Value="6,3"/>
    </Style>

    <!-- ═══════════════════ MithrilTabItem ═══════════════════ -->

    <!-- Themed TabItem with optional notification badge. Set
         c:TabBadge.Count="<int>" on a TabItem to render a small accent pill
         next to the header; the pill is hidden when Count <= 0.

         Apply via TabControl's ItemContainerStyle when binding ItemsSource
         to a collection of ModuleTab — generated TabItem containers honor
         the style cleanly. (Items added directly to TabControl.Items via
         XAML or code bypass the ItemContainerStyle pipeline; that's the
         WPF quirk #233 was unwinding.) -->
    <Style x:Key="MithrilTabItemStyle" TargetType="TabItem">
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}"/>
        <Setter Property="FontSize"   Value="{DynamicResource AppFontSizeMedium}"/>
        <Setter Property="Padding"    Value="14,6"/>
        <Setter Property="Margin"    Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="Bd" Background="Transparent" Padding="{TemplateBinding Padding}"
                            BorderBrush="Transparent" BorderThickness="0,0,0,2" Margin="0,0,2,0">
                        <StackPanel Orientation="Horizontal">
                            <ContentPresenter ContentSource="Header" VerticalAlignment="Center"/>
                            <Border x:Name="BadgePill" Margin="6,0,0,0" Padding="5,0"
                                    MinWidth="16" Height="14" CornerRadius="7"
                                    Background="{StaticResource AccentBrush}"
                                    VerticalAlignment="Center"
                                    Visibility="{Binding Path=(c:TabBadge.Count), RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource PositiveIntToVis}}">
                                <TextBlock Text="{Binding Path=(c:TabBadge.Count), RelativeSource={RelativeSource TemplatedParent}}"
                                           Foreground="#FF1A1A1A" FontWeight="SemiBold"
                                           FontSize="{DynamicResource AppFontSizeXSmall}"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#18FFFFFF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Default DataTemplate for ModuleTab tab-strip headers. Binds Font* and
         Foreground via RelativeSource to the ancestor TabItem so the header
         text follows MithrilTabItemStyle (and its IsSelected trigger) rather
         than picking up the implicit TextBlock style's AppFontSize/TextPrimary,
         which would otherwise pin every tab header to 12pt and full-bright
         regardless of selection. -->
    <DataTemplate x:Key="ModuleTabHeaderTemplate">
        <TextBlock Text="{Binding Header}"
                   FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=TabItem}}"
                   FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType=TabItem}}"
                   Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=TabItem}}"/>
    </DataTemplate>

    <!-- ═══════════════════ MithrilQueryBox ═══════════════════ -->

    <!-- Highlight brushes — reused by MithrilQueryBox when coloring tokens. -->
    <SolidColorBrush x:Key="QueryColumnBrush"   Color="#FFD4A847"/>
    <SolidColorBrush x:Key="QueryKeywordBrush"  Color="#FFCFE8FF"/>
    <SolidColorBrush x:Key="QueryOperatorBrush" Color="#FFAAAAAA"/>
    <SolidColorBrush x:Key="QueryStringBrush"   Color="#FF7AB880"/>
    <SolidColorBrush x:Key="QueryNumberBrush"   Color="#FFE0BF7A"/>
    <SolidColorBrush x:Key="QueryDurationBrush" Color="#FFE0BF7A"/>
    <SolidColorBrush x:Key="QueryPunctBrush"    Color="#FF999999"/>
    <SolidColorBrush x:Key="QueryErrorBrush"    Color="#FFCC6666"/>

    <Style TargetType="{x:Type c:MithrilQueryBox}">
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type c:MithrilQueryBox}">
                    <Border Background="{StaticResource BgSurfaceBrush}"
                            BorderBrush="{StaticResource BorderSubtleBrush}"
                            BorderThickness="1"
                            CornerRadius="3"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <TextBlock x:Name="PART_Overlay"
                                       Margin="5,3"
                                       FontFamily="{DynamicResource AppMonoFontFamily}"
                                       FontSize="{DynamicResource AppFontSize}"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"
                                       TextWrapping="NoWrap"
                                       TextTrimming="None"
                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBox x:Name="PART_Editor"
                                     Padding="5,3"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     FontFamily="{DynamicResource AppMonoFontFamily}"
                                     FontSize="{DynamicResource AppFontSize}"
                                     Foreground="Transparent"
                                     CaretBrush="{StaticResource TextPrimaryBrush}"
                                     SelectionBrush="{StaticResource AccentBrush}"
                                     SelectionOpacity="0.35"
                                     VerticalContentAlignment="Center"
                                     AcceptsReturn="False"
                                     AcceptsTab="False"
                                     AutoWordSelection="True"/>
                            <TextBlock x:Name="Watermark"
                                       Margin="6,3"
                                       FontFamily="{DynamicResource AppMonoFontFamily}"
                                       FontSize="{DynamicResource AppFontSize}"
                                       Foreground="{StaticResource TextTertiaryBrush}"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"
                                       Text="{TemplateBinding Watermark}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, ElementName=PART_Editor}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <Popup x:Name="PART_Popup"
                                   Placement="Relative"
                                   StaysOpen="False"
                                   AllowsTransparency="True"
                                   PopupAnimation="Fade">
                                <Border Background="{StaticResource BgSurfaceBrush}"
                                        BorderBrush="{StaticResource BorderStrongBrush}"
                                        BorderThickness="1"
                                        CornerRadius="0,0,3,3"
                                        SnapsToDevicePixels="True">
                                    <ListBox x:Name="PART_List"
                                             Background="Transparent"
                                             Foreground="{StaticResource TextPrimaryBrush}"
                                             BorderThickness="0"
                                             MaxHeight="240"
                                             MinWidth="260"
                                             FontFamily="{DynamicResource AppMonoFontFamily}"
                                             FontSize="{DynamicResource AppFontSize}"
                                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                        <ListBox.ItemContainerStyle>
                                            <Style TargetType="ListBoxItem">
                                                <Setter Property="Padding" Value="6,3"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ListBoxItem">
                                                            <Border x:Name="Bd"
                                                                    Background="Transparent"
                                                                    Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsSelected" Value="True">
                                                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentSoftBrush}"/>
                                                                </Trigger>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgSurfaceHoverBrush}"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </ListBox.ItemContainerStyle>
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{Binding Kind}"
                                                               FontSize="{DynamicResource AppFontSizeSmall}" Foreground="{StaticResource TextTertiaryBrush}"
                                                               VerticalAlignment="Center" Width="58"/>
                                                    <TextBlock Grid.Column="1" Text="{Binding Label}"
                                                               VerticalAlignment="Center"/>
                                                    <TextBlock Grid.Column="2" Text="{Binding Detail}"
                                                               FontSize="{DynamicResource AppFontSizeSmall}" Foreground="{StaticResource TextTertiaryBrush}"
                                                               VerticalAlignment="Center" Margin="8,0,0,0"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══════════════════ Toolbar (Sort / Filter / View) ═══════════════════ -->

    <!-- Compact icon-with-label pill used by the Sort and Filter toolbar buttons.
         Smaller than the default Button (MinHeight 22 vs 24, padding 8,2 vs 10,4)
         so a row of these doesn't overpower the master list above which they sit. -->
    <Style x:Key="MithrilToolbarButtonStyle" TargetType="Button">
        <Setter Property="Background"  Value="Transparent"/>
        <Setter Property="Foreground"  Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="Padding"     Value="8,2"/>
        <Setter Property="MinHeight"   Value="22"/>
        <Setter Property="Cursor"      Value="Hand"/>
        <Setter Property="FontFamily"  Value="{DynamicResource AppFontFamily}"/>
        <Setter Property="FontSize"    Value="{DynamicResource AppFontSizeHint}"/>
        <Setter Property="Focusable"   Value="True"/>
        <Setter Property="IsTabStop"   Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="3"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                          RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgSurfaceHoverBrush}"/>
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource BorderStrongBrush}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentSoftBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══════════════════ ViewModeToggle ═══════════════════ -->

    <!-- Segmented toggle backed by an internal ListBox. Each ListBoxItem renders a
         ViewModeOption's Icon (and DisplayName when present); the selected segment
         picks up AccentBrush on its border + foreground. ListBox.SelectedValuePath
         points at ViewModeOption.Id so the host VM binds a plain string. -->
    <Style TargetType="{x:Type c:ViewModeToggle}">
        <Setter Property="Background"  Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="Focusable"   Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type c:ViewModeToggle}">
                    <ListBox x:Name="PART_List"
                             ItemsSource="{Binding Modes, RelativeSource={RelativeSource TemplatedParent}}"
                             SelectedValue="{Binding SelectedMode, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                             SelectedValuePath="Id"
                             Background="Transparent"
                             BorderThickness="0"
                             Padding="0"
                             KeyboardNavigation.TabNavigation="Once"
                             SnapsToDevicePixels="True">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Margin" Value="0,0,2,0"/>
                                <Setter Property="Padding" Value="6,2"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                <Setter Property="ToolTip" Value="{Binding ToolTipText}"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="Bd"
                                                    Background="Transparent"
                                                    BorderBrush="{StaticResource BorderSubtleBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="3"
                                                    Padding="{TemplateBinding Padding}"
                                                    SnapsToDevicePixels="True">
                                                <StackPanel Orientation="Horizontal">
                                                    <ContentPresenter Content="{Binding Icon}"
                                                                      VerticalAlignment="Center"
                                                                      HorizontalAlignment="Center"/>
                                                    <TextBlock Text="{Binding DisplayName}"
                                                               Margin="4,0,0,0"
                                                               VerticalAlignment="Center"
                                                               Foreground="{TemplateBinding Foreground}">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding DisplayName}" Value="">
                                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </StackPanel>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgSurfaceHoverBrush}"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                                    <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                                </Trigger>
                                                <Trigger Property="IsKeyboardFocused" Value="True">
                                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- Wraps an entity detail card. Owns (a) the top-right "Copy as image" camera
         button and (b) the mono bottom-right internal-name footer — both kept off
         the individual views for consistency. The outer Grid is Left/Top-aligned so
         it sizes to the card's desired extent (no pane filler → the snapshot is
         tightly sized); PART_ExportContent (footer + body) is the capture target;
         the camera button is its sibling so it is excluded from the capture. See
         DetailExportHost.cs (mirrors the ViewModeToggle pattern above). -->
    <Style TargetType="{x:Type c:DetailExportHost}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type c:DetailExportHost}">
                    <Grid>
                        <!-- PART_ExportContent is Top-aligned so the footer sits directly
                             under the body (no pane filler) in-app AND in the capture;
                             width stays Stretch so the inline card keeps a stable
                             pane-width footprint (no per-selection width jiggle). The
                             exporter crops to drawn-content bounds, so the capture is
                             tight regardless of this Stretch width. -->
                        <DockPanel x:Name="PART_ExportContent" VerticalAlignment="Top">
                            <!-- Internal-name footer: mono, bottom-right, hidden when
                                 FooterText is empty (NullOrEmptyToVis is string-only —
                                 FooterText is a string, so it is correct here). Click
                                 copies the text; the label flips to a "copied" ack for
                                 ~1.2s (driven by the read-only FooterCopied DP). -->
                            <Button x:Name="PART_FooterButton" DockPanel.Dock="Bottom"
                                    HorizontalAlignment="Right" Cursor="Hand"
                                    Background="Transparent" BorderThickness="0" Padding="0"
                                    Margin="0,8,14,12"
                                    ToolTip="Click to copy the internal name">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Visibility"
                                                Value="{Binding FooterText, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource NullOrEmptyToVis}}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasFooterSegments, RelativeSource={RelativeSource TemplatedParent}}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Button.Template>
                                <TextBlock FontFamily="{DynamicResource AppMonoFontFamily}"
                                           FontSize="{DynamicResource AppFontSizeSmall}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="{Binding FooterText, RelativeSource={RelativeSource TemplatedParent}}"/>
                                            <Setter Property="Foreground" Value="#88FFFFFF"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding FooterCopied, RelativeSource={RelativeSource TemplatedParent}}" Value="True">
                                                    <Setter Property="Text" Value="copied ✓"/>
                                                    <Setter Property="Foreground" Value="#FFD4A847"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Button>
                            <!-- Segment footer (#Lorebook): N independent click-to-copy
                                 chips joined by an inert middot. Shown only when
                                 FooterSegments is set; mutually exclusive with
                                 PART_FooterButton (exactly one is non-Collapsed).
                                 NOTE: declaration order here is load-bearing — these
                                 two are sibling Dock="Bottom" elements; the contract
                                 is that only one is ever visible. Don't insert another
                                 Dock="Bottom" sibling between them or make both visible.
                                 Single-line → export height unchanged. -->
                            <ItemsControl x:Name="FooterSegmentsItemsControl"
                                          DockPanel.Dock="Bottom"
                                          HorizontalAlignment="Right"
                                          Margin="0,8,14,12"
                                          ItemsSource="{Binding FooterSegmentItems, RelativeSource={RelativeSource TemplatedParent}}"
                                          Visibility="{Binding HasFooterSegments, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVis}}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <!-- All three row items (Ellipse + both chip Buttons)
                                                 are VerticalAlignment=Center so the dot sits at
                                                 the text's optical centre. Without centring, the
                                                 chip text stretches and draws from the top of its
                                                 line box while a centred dot lands near the
                                                 baseline — reading as a period. The separator is a
                                                 shape, not a glyph, so it is also font-independent. -->
                                            <Ellipse Width="3" Height="3" Margin="7,0"
                                                     VerticalAlignment="Center" Fill="#88FFFFFF">
                                                <Ellipse.Style>
                                                    <Style TargetType="Ellipse">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsFirst}" Value="True">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Ellipse.Style>
                                            </Ellipse>
                                            <Button Command="{Binding CopyCommand}" Cursor="Hand"
                                                    Background="Transparent" BorderThickness="0"
                                                    Padding="0" VerticalAlignment="Center"
                                                    ToolTip="Click to copy">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <ContentPresenter/>
                                                    </ControlTemplate>
                                                </Button.Template>
                                                <TextBlock FontFamily="{DynamicResource AppMonoFontFamily}"
                                                           FontSize="{DynamicResource AppFontSizeSmall}"
                                                           VerticalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Text" Value="{Binding Text}"/>
                                                            <Setter Property="Foreground" Value="#88FFFFFF"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Copied}" Value="True">
                                                                    <Setter Property="Text" Value="copied ✓"/>
                                                                    <Setter Property="Foreground" Value="#FFD4A847"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <ContentPresenter/>
                        </DockPanel>
                        <Button x:Name="PART_ExportButton"
                                HorizontalAlignment="Right" VerticalAlignment="Top"
                                Width="24" Height="24" Padding="0" Margin="6"
                                Panel.ZIndex="1"
                                Background="Transparent" BorderThickness="0" Cursor="Hand"
                                ToolTip="Copy this card as an image">
                            <icon:PackIconLucide Kind="Camera" Width="14" Height="14"
                                                 Foreground="#66FFFFFF"/>
                        </Button>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== Link · the Phase-4 shared navigation primitive (G3 grammar:
         "Link · navigates · V2"). Subsumes EntityChip + ItemSourceChip;
         DataContext is a LinkVm. Mirrors the DetailExportHost templated-control
         pattern above (PART_-named child wired in Link.cs OnApplyTemplate).

         Visual target STRICTLY per docs/silmarillion-visual-grammar.md:
           · No border, no surface/background AT REST.
           · 12px LEAD Lucide glyph before the name (hidden for LinkGlyph.None).
           · Name: AccentBrush gold, body weight, AppFontSize.
           · Trailing ProvenanceSuffix / KindLabel: italic, TextQuaternaryBrush,
             ~10pt (AppFontSizeSmall), hidden when null via NullToVis (the
             object-safe converter — NullOrEmptyToVis is the string-only repo
             footgun and is deliberately NOT used here).
           · Navigable hover: 10% gold tint #1AD4A847 over GrammarRadiusMd,
             Margin 0,-2 tap padding so the tint box fits; Cursor=Hand.
           · G-c pending (IsNavigable=false): rest state IDENTICAL to navigable
             (same gold, same glyph — zero shipping-schedule leak; NOT greyed).
             Difference shows only on hover: a NEUTRAL tint (#14FFFFFF, not gold)
             plus a trailing 11px Copy glyph; click copies DisplayName (handled
             in Link.cs) and flips to a transient "copied" ack (Copied DP). ===== -->
    <Style TargetType="{x:Type c:Link}">
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type c:Link}">
                    <Button x:Name="PART_Button"
                            HorizontalAlignment="Left"
                            Background="Transparent" BorderThickness="0" Padding="0"
                            Margin="0,-2,0,-2">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <!-- Hover tint box. Transparent at rest (grammar: no
                                     surface at rest). IsMouseOver swaps the fill:
                                     navigable → 10% gold; pending → neutral white. -->
                                <Border x:Name="HoverBox"
                                        Background="Transparent"
                                        CornerRadius="{StaticResource GrammarRadiusMd}"
                                        Padding="2,0">
                                    <StackPanel Orientation="Horizontal">
                                        <!-- LEAD element · G3 hybrid icon family
                                             (amend 1) + em-relative size (amend 2,
                                             SUPERSEDES amend-1's fixed 12px). Two
                                             mutually-exclusive elements in the one lead
                                             slot: a real CDN sprite (preferred when
                                             IconId>0 / HasSprite) ELSE the type-coded
                                             Lucide fallback. Switch is HasSprite
                                             (LinkVm bool: int>0, object-safe — no
                                             converter) via the DataTriggers below.
                                             SIZE: em-relative — Width/Height bind the
                                             Link's INHERITED FontSize (AncestorType
                                             c:Link) through FontSizeTimes with the
                                             ratified per-family factor. Defaults are
                                             the Prose factors (sprite ×1.0, Lucide
                                             ×0.75); the Density=List trigger swaps to
                                             the list factors (×1.5 / ×1.125). Mirrors
                                             Link.LeadFactor (unit-tested). -->
                                        <!-- Preferred: real game-art sprite. Collapsed
                                             by default; the HasSprite=True trigger
                                             reveals it (and hides the Lucide). IconImage
                                             self-collapses internally if IconId<=0.
                                             NearestNeighbor = the WPF equivalent of the
                                             grammar's image-rendering: pixelated, so
                                             pixel-art stays crisp at any em scale. -->
                                        <c:IconImage x:Name="LeadSprite"
                                            Width="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type c:Link}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=1.0}"
                                            Height="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type c:Link}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=1.0}"
                                            RenderOptions.BitmapScalingMode="NearestNeighbor"
                                            Margin="0,0,4,0"
                                            VerticalAlignment="Center"
                                            IconId="{Binding IconId}"
                                            Visibility="Collapsed"/>
                                        <!-- Fallback: em-relative LEAD Lucide. Collapsed
                                             for LinkGlyph.None (LinkGlyphToVis); ALSO
                                             hidden when a sprite is present
                                             (HasSprite=True trigger). -->
                                        <icon:PackIconLucide x:Name="LeadLucide"
                                            Width="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type c:Link}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=0.75}"
                                            Height="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type c:Link}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=0.75}"
                                            Margin="0,0,4,0"
                                            VerticalAlignment="Center"
                                            Foreground="{StaticResource AccentBrush}"
                                            Kind="{Binding Glyph, Converter={StaticResource LinkGlyphToLucideKind}}"
                                            Visibility="{Binding Glyph, Converter={StaticResource LinkGlyphToVis}}"/>
                                        <!-- Name: gold, body weight, inherited (em)
                                             FontSize — the text itself already scales
                                             with the Appearance slider via inheritance;
                                             no converter needed at 1em. -->
                                        <TextBlock Text="{Binding DisplayName}"
                                                   Foreground="{StaticResource AccentBrush}"
                                                   FontWeight="Normal"
                                                   VerticalAlignment="Center"/>
                                        <!-- Trailing ProvenanceSuffix: italic, quaternary,
                                             ~10pt, hidden when null (NullToVis, object-safe). -->
                                        <TextBlock Text="{Binding ProvenanceSuffix}"
                                                   Margin="5,0,0,0"
                                                   Foreground="{StaticResource TextQuaternaryBrush}"
                                                   FontSize="{DynamicResource AppFontSizeSmall}"
                                                   FontStyle="Italic"
                                                   VerticalAlignment="Center"
                                                   Visibility="{Binding ProvenanceSuffix, Converter={StaticResource NullToVis}}"/>
                                        <!-- Trailing KindLabel: same slot, same rule. -->
                                        <TextBlock Text="{Binding KindLabel}"
                                                   Margin="5,0,0,0"
                                                   Foreground="{StaticResource TextQuaternaryBrush}"
                                                   FontSize="{DynamicResource AppFontSizeSmall}"
                                                   FontStyle="Italic"
                                                   VerticalAlignment="Center"
                                                   Visibility="{Binding KindLabel, Converter={StaticResource NullToVis}}"/>
                                        <!-- G-c pending copy affordance: 11px Copy glyph,
                                             revealed only on hover of a non-navigable Link
                                             (HoverCopyGlyph trigger below). Hidden by
                                             default and entirely absent for navigable. -->
                                        <icon:PackIconLucide x:Name="CopyGlyph"
                                            Kind="Copy"
                                            Width="11" Height="11"
                                            Margin="5,0,0,0"
                                            VerticalAlignment="Center"
                                            Foreground="{StaticResource TextTertiaryBrush}"
                                            Visibility="Collapsed"/>
                                        <!-- Transient "copied" ack (G-c useful action),
                                             driven by the read-only Copied DP set in
                                             Link.cs for ~1.2s after a pending click. -->
                                        <TextBlock x:Name="CopiedAck"
                                                   Text="copied ✓"
                                                   Margin="5,0,0,0"
                                                   Foreground="{StaticResource AccentBrush}"
                                                   FontSize="{DynamicResource AppFontSizeSmall}"
                                                   VerticalAlignment="Center"
                                                   Visibility="Collapsed"/>
                                    </StackPanel>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <!-- G3 hybrid lead switch: a real sprite present
                                         (HasSprite = IconId>0) wins — reveal the sprite,
                                         hide the Lucide fallback. When False, both
                                         elements keep their default visibility (sprite
                                         Collapsed; Lucide per LinkGlyphToVis), i.e. the
                                         pre-amendment Lucide-only behaviour. -->
                                    <DataTrigger Binding="{Binding HasSprite}" Value="True">
                                        <Setter TargetName="LeadSprite" Property="Visibility" Value="Visible"/>
                                        <Setter TargetName="LeadLucide" Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <!-- G3-amend-2 Density=List: swap the lead em
                                         factors from the Prose defaults (sprite ×1.0,
                                         Lucide ×0.75) to the list factors (×1.5 /
                                         ×1.125). Density is a DP on the c:Link
                                         ancestor (mirrors the Copied AncestorType
                                         binding below); the size still binds the
                                         Link's inherited FontSize so it stays
                                         em-relative. Exactly Link.LeadFactor. -->
                                    <DataTrigger Binding="{Binding Density, RelativeSource={RelativeSource AncestorType={x:Type c:Link}}}" Value="List">
                                        <Setter TargetName="LeadSprite" Property="Width"
                                                Value="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type c:Link}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=1.5}"/>
                                        <Setter TargetName="LeadSprite" Property="Height"
                                                Value="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type c:Link}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=1.5}"/>
                                        <Setter TargetName="LeadLucide" Property="Width"
                                                Value="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type c:Link}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=1.125}"/>
                                        <Setter TargetName="LeadLucide" Property="Height"
                                                Value="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type c:Link}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=1.125}"/>
                                    </DataTrigger>
                                    <!-- Navigable hover: 10% gold tint + Hand cursor. -->
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsNavigable}" Value="True"/>
                                            <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter TargetName="HoverBox" Property="Background" Value="#1AD4A847"/>
                                    </MultiDataTrigger>
                                    <DataTrigger Binding="{Binding IsNavigable}" Value="True">
                                        <Setter Property="Cursor" Value="Hand"/>
                                    </DataTrigger>
                                    <!-- Pending hover: neutral (non-gold) tint + reveal
                                         the trailing Copy glyph. Rest state is untouched
                                         → identical to navigable (zero schedule leak). -->
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsNavigable}" Value="False"/>
                                            <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter TargetName="HoverBox" Property="Background" Value="#14FFFFFF"/>
                                        <Setter TargetName="CopyGlyph" Property="Visibility" Value="Visible"/>
                                    </MultiDataTrigger>
                                    <DataTrigger Binding="{Binding IsNavigable}" Value="False">
                                        <Setter Property="Cursor" Value="Hand"/>
                                    </DataTrigger>
                                    <!-- Copied ack: swap the copy glyph for the ack text
                                         while the Copied DP is set. -->
                                    <DataTrigger Binding="{Binding Copied, RelativeSource={RelativeSource AncestorType={x:Type c:Link}}}" Value="True">
                                        <Setter TargetName="CopyGlyph" Property="Visibility" Value="Collapsed"/>
                                        <Setter TargetName="CopiedAck" Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== SetRef · the Phase-4 shared Set-reference primitive (G3 grammar:
         "Set-reference · filter / keyword / group / stacking" + carry-forward #4).
         One polymorphic control carrying BOTH shapes on one blue chassis;
         DataContext is a SetRefVm. Mirrors the Link templated-control pattern
         above (PART_-named child wired in SetRef.cs OnApplyTemplate).

         Visual target STRICTLY per docs/silmarillion-visual-grammar.md
         (every value is a committed token — none invented here):
           · A blue chip WITH a chassis (unlike Link): idle fill
             SetRefIdleFillBrush, 1px SetRefBorderBrush border,
             GrammarRadiusSm (2) corner, 1×8 padding, AppFontSizeHint text in
             SetRefTextBrush.
           · summary-form (MatchCount != null) → DisplayText is
             "{Label} · {count} →"; tag-form (MatchCount == null) → bare Label
             (no count, no arrow, NO lead glyph — grammar "glyph: None by
             default"). The VM bakes the shape into DisplayText so the template
             binds one string and the two shapes never fork.
           · Optional leading SlotOrdinal prefix (stacking semantics): small,
             TextQuaternaryBrush, hidden when null via the VM bool HasOrdinal
             (a bool, NOT a converter — dodges the string-only NullOrEmptyToVis
             repo footgun and avoids a bespoke int?->Visibility converter).
           · Hover: fill darkens toward AccentSoftBrush (active fill), border
             brightens to BorderStrongBrush — the drawer-pulling feel.
             Cursor=Hand ONLY when IsActionable.
           · AVAILABILITY COROLLARY (never-grey-pill guarantee): IsActionable
             changes interaction ONLY. The at-rest chassis (fill/border/text/
             radius/padding) is IDENTICAL regardless of IsActionable — an
             un-wired tag-form Set-ref stays on the FULL blue chassis and is
             NEVER demoted to an inert grey Fact pill (the forbidden inverted-
             affordance lie). The only IsActionable-conditioned setters are the
             hover fill/border and Cursor; none touch the rest state. ===== -->
    <Style TargetType="{x:Type c:SetRef}">
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type c:SetRef}">
                    <Button x:Name="PART_Button"
                            HorizontalAlignment="Left"
                            Background="Transparent" BorderThickness="0" Padding="0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <!-- Row = ordinal list-marker OUTSIDE the chassis
                                     (dim mono, left gutter ~8px, ordered-list
                                     style — ratified specimen) + the blue chip
                                     box. The ordinal is NOT inside the rectangle. -->
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock x:Name="OrdinalPrefix"
                                               Text="{Binding OrdinalText}"
                                               Margin="0,0,8,0"
                                               Foreground="{StaticResource TextQuaternaryBrush}"
                                               FontFamily="{DynamicResource AppMonoFontFamily}"
                                               FontSize="{DynamicResource AppFontSizeXSmall}"
                                               VerticalAlignment="Center"/>
                                    <!-- The chassis. Rest state is token-fixed and
                                         does NOT vary with IsActionable
                                         (availability corollary). Hover-only
                                         setters below. Holds only the shape-baked
                                         body: tag-form = bare Label; summary-form
                                         = "{Label} · N matches →". -->
                                    <Border x:Name="Chassis"
                                            Background="{StaticResource SetRefIdleFillBrush}"
                                            BorderBrush="{StaticResource SetRefBorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="{StaticResource GrammarRadiusSm}"
                                            Padding="8,1">
                                        <TextBlock Text="{Binding DisplayText}"
                                                   Foreground="{StaticResource SetRefTextBrush}"
                                                   FontSize="{DynamicResource AppFontSizeHint}"
                                                   VerticalAlignment="Center"/>
                                    </Border>
                                </StackPanel>
                                <ControlTemplate.Triggers>
                                    <!-- Hide the ordinal prefix entirely when the VM
                                         carries no SlotOrdinal (positionally inert /
                                         single). VM bool, not a converter. -->
                                    <DataTrigger Binding="{Binding HasOrdinal}" Value="False">
                                        <Setter TargetName="OrdinalPrefix" Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <!-- Actionable: hand cursor + drawer-pulling
                                         hover (fill -> active, border -> strong).
                                         Rest state untouched. -->
                                    <DataTrigger Binding="{Binding IsActionable}" Value="True">
                                        <Setter Property="Cursor" Value="Hand"/>
                                    </DataTrigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsActionable}" Value="True"/>
                                            <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter TargetName="Chassis" Property="Background" Value="{StaticResource AccentSoftBrush}"/>
                                        <Setter TargetName="Chassis" Property="BorderBrush" Value="{StaticResource BorderStrongBrush}"/>
                                    </MultiDataTrigger>
                                    <!-- IsActionable=false: NO hover/cursor setters
                                         at all. The chassis above is the entire look
                                         (identical to actionable at rest) — never a
                                         grey pill. -->
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== FactTable · the Phase-4 shared Fact-group primitive (G3 grammar:
         "Fact · inert · weight-axis" + carry-forward #3). ONE polymorphic
         control rendering a label/value group in THREE layouts from one
         FactTableVm; DataContext is a FactTableVm. Mirrors the Link/SetRef
         templated-control pattern above, but Fact is INERT — no PART_Button, no
         ClickCommand, no hover, zero interactivity.

         Visual target STRICTLY per docs/silmarillion-visual-grammar.md
         (every value is a committed token — none invented here):
           · Value text  = TextPrimaryBrush   (NEVER gold — G-b strips gold
             from every Fact value; the model carries no brush at all).
           · Label text  = TextTertiaryBrush  (Structure-weight label).
           · NO border, NO surface/background anywhere (Fact is the only fully
             inert tier).
           · ANTI-FORK (carry-forward #3): this is ONE control. The Style binds
             the control's Layout DP to the VM's Layout and DataTriggers the
             presented template off it — Strip / Grid / Scalar are three
             ContentControl templates selected by ONE trigger set, NOT three
             forked controls (same rationale as the single-Link mandate).
           · Strip  → ItemsControl-free single TextBlock bound to the VM's pure
             StripText helper: "Label Value · Label Value · …". The middot is
             injected BETWEEN segments by StripText only — it is NOT part of any
             value (grammar: "inline dot separators that are NOT part of any
             value"). One bound string ⇒ separator logic is unit-tested in
             FactTableVm.StripText, never re-decided in XAML.
           · Grid   → vertical 2-column ItemsControl: a Grid per row, label
             (TextTertiaryBrush, left) + value (TextPrimaryBrush, right-aligned,
             tabular-style alignment via a shared SharedSizeGroup label column so
             values line up). One Label … Value per row, pair order preserved.
           · Scalar → a single bare value TextBlock, no label, no rows (the
             degenerate case the carry-forward demands the SAME primitive
             degrade to — Pairs has exactly one label-less entry).
           · Quiet (footer-quiet weight, orthogonal/P2): when the VM's Quiet is
             true the value run switches to AppMonoFontFamily + TextTertiaryBrush
             (the InternalName footer-quiet case). Default OFF — weight is not
             over-engineered here. ===== -->
    <Style TargetType="{x:Type c:FactTable}">
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <!-- The VM is the single source of truth: mirror its Layout onto the
             control's Layout DP so the DataTriggers below have a control-level
             enum target. One control, layout-switched — not a fork. -->
        <Setter Property="Layout" Value="{Binding Layout}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type c:FactTable}">
                    <!-- Grid.IsSharedSizeScope so the Grid-layout rows share a
                         label-column width ⇒ values right-align on a common
                         column (tabular-nums-style alignment). The three layout
                         views live directly in this ONE template; the trigger
                         set below toggles which is visible — that IS the
                         anti-fork (one control, not three). -->
                    <Grid x:Name="Root" Grid.IsSharedSizeScope="True">
                        <!-- STRIP: one TextBlock bound to the pure StripText
                             helper (separator injected between segments only). -->
                        <!-- G3-amend-2: stat-strip text = 0.9em (was the fixed
                             AppFontSizeHint=11). EM-SOURCE: the c:FactTable
                             ancestor's inherited FontSize (FactTable sets no
                             FontSize itself → no cycle, AncestorType not Self).
                             ×0.9 via FontSizeTimes. -->
                        <TextBlock x:Name="StripView"
                                   Visibility="Collapsed"
                                   Text="{Binding StripText}"
                                   TextWrapping="Wrap"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   FontFamily="{DynamicResource AppFontFamily}"
                                   FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type c:FactTable}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=0.9}"/>

                        <!-- GRID: vertical 2-column, label left, value
                             right-aligned on a shared column. -->
                        <ItemsControl x:Name="GridView"
                                      Visibility="Collapsed"
                                      ItemsSource="{Binding Pairs}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" SharedSizeGroup="FactLabel"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding Label}"
                                                   Margin="0,0,16,0"
                                                   Foreground="{StaticResource TextTertiaryBrush}"
                                                   FontFamily="{DynamicResource AppFontFamily}"
                                                   FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type c:FactTable}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=0.9}"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Value}"
                                                   HorizontalAlignment="Right"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   FontFamily="{DynamicResource AppFontFamily}"
                                                   FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type c:FactTable}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=0.9}"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- SCALAR: a single bare value, no label, no rows.
                             Pairs[0].Value is the lone label-less entry. -->
                        <TextBlock x:Name="ScalarView"
                                   Visibility="Collapsed"
                                   Text="{Binding Pairs[0].Value}"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   FontFamily="{DynamicResource AppFontFamily}"
                                   FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type c:FactTable}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=0.9}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- ONE trigger set selects the layout view. This IS the
                             anti-fork: three templates, one control, switched by
                             the Layout DP (mirrored from the VM). -->
                        <Trigger Property="Layout" Value="Strip">
                            <Setter TargetName="StripView" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="Layout" Value="Grid">
                            <Setter TargetName="GridView" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="Layout" Value="Scalar">
                            <Setter TargetName="ScalarView" Property="Visibility" Value="Visible"/>
                        </Trigger>

                        <!-- Footer-quiet weight (orthogonal/P2): mono +
                             TextTertiaryBrush on the value run. Applies to the
                             single-value views (Strip/Scalar); Grid stays at
                             body weight. Default OFF (Quiet=false). -->
                        <DataTrigger Binding="{Binding Quiet}" Value="True">
                            <Setter TargetName="StripView" Property="FontFamily" Value="{DynamicResource AppMonoFontFamily}"/>
                            <Setter TargetName="StripView" Property="Foreground" Value="{StaticResource TextTertiaryBrush}"/>
                            <Setter TargetName="ScalarView" Property="FontFamily" Value="{DynamicResource AppMonoFontFamily}"/>
                            <Setter TargetName="ScalarView" Property="Foreground" Value="{StaticResource TextTertiaryBrush}"/>
                            <!-- G3-amend-2: footer-quiet weight = the Footer-ID
                                 tier size, 0.78em (was the fixed AppFontSizeSmall=
                                 10). Same em-source as the strip (c:FactTable
                                 ancestor), ×0.78. -->
                            <Setter TargetName="StripView" Property="FontSize" Value="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type c:FactTable}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=0.78}"/>
                            <Setter TargetName="ScalarView" Property="FontSize" Value="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type c:FactTable}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=0.78}"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== FactFooter · the Phase-4 shared footer-ID primitive (G3 grammar:
         "G-a — Fact identifiers, copyable without becoming Control"). 0/1/2
         FactFooterId cells; DataContext is a FactFooterVm. COEXISTS with
         DetailExportHost's legacy footer (Phase 5 migrates hosts) — this is the
         standalone G-a primitive only. Mirrors the FactTable templated-control
         pattern above (DefaultStyleKeyProperty.OverrideMetadata in FactFooter.cs;
         per-cell ButtonBase wired in OnApplyTemplate, not a single named PART).

         Visual target STRICTLY per docs/silmarillion-visual-grammar.md
         (every value is a committed token — none invented here):
           · LOCATION, NOT CHASSIS: the strip sits BELOW a thin 1px
             BorderFaintBrush TOP divider, beneath the read-flow. NO surface,
             NO fill, NO border other than that one top divider. This is a Fact,
             not a Control — it declares itself only on contact.
           · Right-aligned (matches the detail-view footer convention), but the
             divider spans the FULL content width (the Border is stretched; the
             cells DockPanel inside is right-aligned).
           · 0 ids → the whole control is Collapsed (HasIds=false → no divider
             with nothing under it). 1 id → a single cell. 2 ids → the two cells
             with an INERT middot `·` between them. The separator is NEVER part
             of any copied value (the first cell suppresses its leading middot
             via the VM IsFirst-style index; the copy payload is the cell Value
             ONLY — mirrors FooterSegmentItem / FactTableVm.StripSeparator).
           · Each cell: tiny UPPERCASE LabelTag in TextQuaternaryBrush at
             AppFontSizeXSmall, then Value in TextTertiaryBrush +
             AppMonoFontFamily at AppFontSizeSmall.
           · COPYABLE cell (Copyable=true — a cross-entity reference KEY): on
             HOVER reveal an 11px Copy PackIconLucide trailing the value;
             Cursor=Hand; click copies EXACTLY that cell's Value (handled in
             FactFooter.cs, try/catch like DetailExportHost) and flips that
             cell's Copied for ~1.2s (per-cell ack — the OTHER cell is
             unaffected, exactly like FooterSegmentItem.Copied). At REST the
             copy glyph is NOT shown (G-a: "declares itself only on contact").
           · INERT cell (Copyable=false — a storage-only ROW): Cursor=Default,
             NO hover state, NO glyph EVER, click does nothing. The Copyable
             bool is the sole discriminator — NOT the LabelTag string. ===== -->
    <ItemsPanelTemplate x:Key="FactFooterCellsPanel">
        <StackPanel Orientation="Horizontal"/>
    </ItemsPanelTemplate>
    <Style TargetType="{x:Type c:FactFooter}">
        <!-- Right-aligned strip (detail-footer convention); the divider Border
             below still stretches the full content width. -->
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type c:FactFooter}">
                    <!-- The ONLY surface: a 1px TOP divider spanning the content
                         width. No fill, no other border (Fact = no chassis). The
                         whole control collapses at 0 ids (no divider with
                         nothing under it). -->
                    <Border x:Name="Root"
                            BorderBrush="{StaticResource BorderFaintBrush}"
                            BorderThickness="0,1,0,0"
                            Padding="0,4,0,0"
                            Background="Transparent">
                        <!-- Cells right-aligned within the full-width divider. -->
                        <ItemsControl x:Name="PART_Cells"
                                      HorizontalAlignment="Right"
                                      AlternationCount="2"
                                      ItemsSource="{Binding Ids}"
                                      ItemsPanel="{StaticResource FactFooterCellsPanel}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <!-- A ButtonBase per cell (FactFooter.cs walks
                                         the realised tree and wires Click). No
                                         chassis: transparent, borderless, no
                                         padding — the button is a hit-target, not
                                         a Control surface. -->
                                    <Button Background="Transparent"
                                            BorderThickness="0"
                                            Padding="0"
                                            HorizontalContentAlignment="Left">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <StackPanel x:Name="CellRoot"
                                                            Orientation="Horizontal"
                                                            Background="Transparent">
                                                    <!-- Inert middot BETWEEN cells
                                                         only; the first cell
                                                         suppresses it. Never part
                                                         of any copy payload. -->
                                                    <!-- G3-amend-2: Footer-ID tier =
                                                         0.78em (was fixed
                                                         AppFontSizeSmall=10). The tiny
                                                         KEY/ROW label is the same
                                                         0.78em (grammar: "the tiny
                                                         KEY/ROW label"), was
                                                         AppFontSizeXSmall=9. EM-SOURCE:
                                                         the c:FactFooter ancestor's
                                                         inherited FontSize (FactFooter
                                                         sets no FontSize → no cycle).
                                                         Sep middot tracks the value
                                                         size so it stays on-line. -->
                                                    <TextBlock x:Name="Sep"
                                                               Text="{x:Static c:FactFooterVm.CellSeparator}"
                                                               Foreground="{StaticResource TextQuaternaryBrush}"
                                                               FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type c:FactFooter}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=0.78}"
                                                               VerticalAlignment="Center"/>
                                                    <!-- Tiny UPPERCASE KEY/ROW tag. -->
                                                    <TextBlock x:Name="Tag"
                                                               Text="{Binding LabelTag}"
                                                               Margin="0,0,4,0"
                                                               Foreground="{StaticResource TextQuaternaryBrush}"
                                                               FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type c:FactFooter}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=0.78}"
                                                               VerticalAlignment="Center"/>
                                                    <!-- Mono value (tertiary). -->
                                                    <TextBlock Text="{Binding Value}"
                                                               Foreground="{StaticResource TextTertiaryBrush}"
                                                               FontFamily="{DynamicResource AppMonoFontFamily}"
                                                               FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type c:FactFooter}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=0.78}"
                                                               VerticalAlignment="Center"/>
                                                    <!-- Hover-revealed 11px Copy
                                                         glyph (copyable only).
                                                         Collapsed at rest (G-a:
                                                         declares itself only on
                                                         contact). -->
                                                    <icon:PackIconLucide x:Name="CopyGlyph"
                                                        Kind="Copy"
                                                        Width="11" Height="11"
                                                        Margin="4,0,0,0"
                                                        VerticalAlignment="Center"
                                                        Foreground="{StaticResource TextTertiaryBrush}"
                                                        Visibility="Collapsed"/>
                                                    <!-- Per-cell transient ack
                                                         (FactFooterId.Copied,
                                                         set in FactFooter.cs). -->
                                                    <TextBlock x:Name="CopiedAck"
                                                               Text="copied ✓"
                                                               Margin="4,0,0,0"
                                                               Foreground="{StaticResource TextTertiaryBrush}"
                                                               FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType={x:Type c:FactFooter}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=0.78}"
                                                               VerticalAlignment="Center"
                                                               Visibility="Collapsed"/>
                                                </StackPanel>
                                                <ControlTemplate.Triggers>
                                                    <!-- First cell → no leading
                                                         middot (separator is
                                                         BETWEEN cells only). -->
                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ContentPresenter}, Path=(ItemsControl.AlternationIndex)}" Value="0">
                                                        <Setter TargetName="Sep" Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <!-- COPYABLE (KEY): hand
                                                         cursor; hover reveals the
                                                         Copy glyph. At rest the
                                                         glyph stays Collapsed. -->
                                                    <DataTrigger Binding="{Binding Copyable}" Value="True">
                                                        <Setter Property="Cursor" Value="Hand"/>
                                                    </DataTrigger>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding Copyable}" Value="True"/>
                                                            <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter TargetName="CopyGlyph" Property="Visibility" Value="Visible"/>
                                                    </MultiDataTrigger>
                                                    <!-- Per-cell ack: swap the
                                                         copy glyph for the ack
                                                         text while Copied is set
                                                         (this cell only). -->
                                                    <DataTrigger Binding="{Binding Copied}" Value="True">
                                                        <Setter TargetName="CopyGlyph" Property="Visibility" Value="Collapsed"/>
                                                        <Setter TargetName="CopiedAck" Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                    <!-- INERT (ROW): Copyable
                                                         false → NO hover/cursor/
                                                         glyph setters at all. The
                                                         default Cursor + Collapsed
                                                         glyph IS the entire look.
                                                         (Defensive: explicit
                                                         default cursor.) -->
                                                    <DataTrigger Binding="{Binding Copyable}" Value="False">
                                                        <Setter Property="Cursor" Value="Arrow"/>
                                                    </DataTrigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- 0 ids → the whole control renders nothing (no divider
                             with nothing under it). G-a: "Strip hidden at 0". -->
                        <DataTrigger Binding="{Binding HasIds}" Value="False">
                            <Setter TargetName="Root" Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== Silmarillion #404 grammar — Fact / Structure / Control shared styles (Phase 4) =====
         Opt-in x:Key'd styles (NOT implicit TargetType overrides) so existing
         hand-authored views are untouched until Phase 5 migrates call sites.
         Values are taken verbatim from docs/silmarillion-visual-grammar.md
         (Fact tier + weight axis; Structure tier; Control tier). No view edited.

         WPF LIMITATION FLAG (not silently dropped, not hacked): the Structure
         tier specifies "tracked uppercase (letter-spacing 0.08em)". WPF
         TextBlock has no native letter-spacing and no CharacterCasing. These
         styles deliver the expressible parts (family / size / weight / pigment
         / margin); the *uppercasing* and *letter-tracking* are deferred to the
         Phase 5 call site (pass already-upper text; tracking would need a
         custom behavior). Surfaced in the Phase 4 reconciliation flags. -->

    <!-- Fact · title-loud: Cambria / 600 / accent, one per pane. G3-amend-2:
         size is 1.5em (was the fixed AppFontSizeHeader=18, which equalled 1.5×
         the AppFontSize=12 default — now it TRACKS the Appearance slider).
         EM-SOURCE MECHANISM (documented per the dispatch): a Style setter cannot
         RelativeSource Self FontSize without a circular self-reference (the
         setter writes FontSize; reading Self.FontSize re-reads the written
         value). So the 1em source is the NEAREST ANCESTOR's inherited
         (TextElement.FontSize) via FindAncestor (FindAncestor skips Self → no
         cycle); that ancestor carries the body em the title would otherwise
         inherit. ×1.5 through FontSizeTimes. -->
    <Style x:Key="FactTitleStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppHeaderFontFamily}"/>
        <Setter Property="FontSize"
                Value="{Binding Path=(TextElement.FontSize), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type FrameworkElement}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=1.5}"/>
        <Setter Property="FontWeight" Value="SemiBold"/><!-- WPF SemiBold == weight 600 -->
        <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <!-- G3-amend-2 NEW: the title-glyph rule. The entity icon shown next to a
         Fact-title is a FIXED 40px IconImage — an "entity stamp", explicitly
         EXEMPT from accessibility (em) scaling (pixel art at non-integer scale
         looks bad; 40px keeps an integer-pixel render). No border, no surface /
         fill: the image just sits in the slot. NearestNeighbor = the WPF
         equivalent of the grammar's image-rendering: pixelated. Opt-in x:Key'd
         (TargetType IconImage) so only the explicitly-tagged header icon picks
         it up; nothing else changes. -->
    <Style x:Key="FactTitleGlyphStyle" TargetType="{x:Type c:IconImage}">
        <Setter Property="Width"  Value="40"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="RenderOptions.BitmapScalingMode" Value="NearestNeighbor"/>
    </Style>

    <!-- Fact · body: Segoe UI 12 / fg-primary. -->
    <Style x:Key="FactBodyStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}"/>
        <Setter Property="FontSize"   Value="{DynamicResource AppFontSize}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <!-- Structure · section label: fg-tertiary, SemiBold, 10px top gap.
         Never gold, no glyph (enforced by being a bare TextBlock style).
         G3-amend-2: size = 0.78em (was the fixed AppFontSizeXSmall=9; only the
         SIZE changes). Same circular-Style-setter constraint as FactTitleStyle
         → 1em source is the NEAREST ANCESTOR's inherited (TextElement.FontSize)
         via FindAncestor (skips Self → no cycle), ×0.78.
         G4 (ratified): the tracked-UPPERCASE treatment (text-transform:uppercase
         + letter-spacing ≈0.08em) is now applied — previously deferred because
         WPF TextBlock has no native letter-spacing. c:StructureLabel.IsEnabled
         rebuilds Inlines as upper-cased chars interleaved with a hair-space
         spacer (em-relative → tracks the Appearance slider). Applied here so all
         ~9 fan-out views inherit it with zero per-call-site work. -->
    <Style x:Key="StructureSectionLabelStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}"/>
        <Setter Property="FontSize"
                Value="{Binding Path=(TextElement.FontSize), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type FrameworkElement}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=0.78}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource TextTertiaryBrush}"/>
        <Setter Property="Margin"     Value="0,10,0,4"/>
        <Setter Property="c:StructureLabel.IsEnabled" Value="True"/>
    </Style>

    <!-- Structure · inline field-label prefix (terminating ':' is the call
         site's text, not the style). Same pigment/weight as section label,
         no top margin (it sits inline before a value). -->
    <Style x:Key="StructureInlinePrefixStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}"/>
        <!-- G3-amend-2: 0.78em (was AppFontSizeXSmall=9). Same FindAncestor
             em-source as StructureSectionLabelStyle (no Style-setter self-cycle). -->
        <Setter Property="FontSize"
                Value="{Binding Path=(TextElement.FontSize), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type FrameworkElement}}, Converter={StaticResource FontSizeTimes}, ConverterParameter=0.78}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource TextTertiaryBrush}"/>
        <!-- G4: same tracked-UPPERCASE behavior as the section label. The
             terminating ':' (call-site text, e.g. "Field:") is preserved
             literally (ToUpperInvariant is a no-op on punctuation) and tracks
             with the letters so the prefix reads as one tracked label. -->
        <Setter Property="c:StructureLabel.IsEnabled" Value="True"/>
    </Style>

    <!-- Structure · group header: dimmer (fg-quaternary), else as section label.
         G4: inherits the tracked-UPPERCASE c:StructureLabel.IsEnabled setter via
         BasedOn StructureSectionLabelStyle (only the pigment is overridden). -->
    <Style x:Key="StructureGroupHeaderStyle" TargetType="TextBlock"
           BasedOn="{StaticResource StructureSectionLabelStyle}">
        <Setter Property="Foreground" Value="{StaticResource TextQuaternaryBrush}"/>
    </Style>

    <!-- Control · the only tier with a real chassis. Opt-in reference style for
         Phase-5 grammar-conformant buttons; existing button styles untouched.
         bg-surface + border-subtle, radius-md 3, 1px, 5x12 pad; hover
         bg-surface-hover / border-strong. (Primary/accent-fill variant is a
         Phase-5 concern — accent-fg token not yet needed; flagged.) -->
    <Style x:Key="GrammarControlChassisStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource BgSurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="Padding" Value="12,5"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Chassis"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource GrammarRadiusMd}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Chassis" Property="Background"
                                    Value="{StaticResource BgSurfaceHoverBrush}"/>
                            <Setter TargetName="Chassis" Property="BorderBrush"
                                    Value="{StaticResource BorderStrongBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
