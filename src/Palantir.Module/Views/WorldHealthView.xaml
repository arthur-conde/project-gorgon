<UserControl x:Class="Palantir.Views.WorldHealthView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Palantir.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:WorldHealthViewModel}"
             Background="Transparent">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Mithril.Shared.Wpf;component/Resources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel MaxWidth="640" HorizontalAlignment="Left" Margin="4,12,4,4">

            <TextBlock Text="Pipeline health"
                       FontFamily="{DynamicResource AppHeaderFontFamily}"
                       FontSize="{DynamicResource AppFontSizeLarge}"
                       FontWeight="SemiBold"
                       Foreground="#FFD4A847" Margin="0,0,0,4"/>

            <TextBlock TextWrapping="Wrap" Foreground="#AAFFFFFF"
                       FontSize="{DynamicResource AppFontSizeHint}"
                       Margin="0,0,0,16">
                Live Arda pipeline health: per-driver mode (replaying / live /
                stalled / halted), frame count, last-seen log timestamp, and
                wall-clock drift. Drift is the gap between the wall-clock and
                the tailer's last poll iteration — a growing drift means the
                polling loop has stopped, not that the user is quiet.
            </TextBlock>

            <!-- Overall status -->
            <Border CornerRadius="4" Padding="14,10" Margin="0,0,0,16">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="#FF2A5A2A"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding AllLive}" Value="False">
                                <Setter Property="Background" Value="#FF5A4A20"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Text="{Binding OverallStatus}" FontWeight="SemiBold"
                           FontSize="{DynamicResource AppFontSizeMedium}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#FF7AE07A"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding AllLive}" Value="False">
                                    <Setter Property="Foreground" Value="#FFE8B04A"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Border>

            <!-- Player driver -->
            <TextBlock Text="PLAYER DRIVER" Foreground="#88FFFFFF"
                       FontSize="{DynamicResource AppFontSizeHint}"
                       FontWeight="SemiBold" Margin="0,0,0,4"/>
            <Border Background="#FF252525" CornerRadius="4" Padding="14,10" Margin="0,0,0,16">
                <StackPanel>
                    <DockPanel Margin="0,0,0,6">
                        <TextBlock DockPanel.Dock="Left" Text="Mode"
                                   Foreground="#CCFFFFFF" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Right"
                                   Text="{Binding PlayerModeText}"
                                   Foreground="#FFFFFFFF" FontWeight="SemiBold"/>
                    </DockPanel>
                    <DockPanel Margin="0,0,0,6">
                        <TextBlock DockPanel.Dock="Left" Text="Frames processed"
                                   Foreground="#CCFFFFFF" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Right"
                                   Text="{Binding PlayerFrames}"
                                   FontFamily="{DynamicResource AppMonoFontFamily}"
                                   Foreground="#FFFFFFFF"/>
                    </DockPanel>
                    <DockPanel Margin="0,0,0,6">
                        <TextBlock DockPanel.Dock="Left" Text="Last log timestamp (UTC)"
                                   Foreground="#88FFFFFF" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Right"
                                   Text="{Binding PlayerLastLog}"
                                   FontFamily="{DynamicResource AppMonoFontFamily}"
                                   Foreground="#88FFFFFF"/>
                    </DockPanel>
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Left" Text="Tailer-poll age"
                                   Foreground="#CCFFFFFF" VerticalAlignment="Center"/>
                        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                            <Ellipse Width="8" Height="8" Fill="#FFE04545"
                                     VerticalAlignment="Center" Margin="0,0,6,0">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding PlayerMode}" Value="Stalled">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding PlayerMode}" Value="Halted">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <TextBlock Text="{Binding PlayerDrift}"
                                       FontFamily="{DynamicResource AppMonoFontFamily}"
                                       Foreground="#FFFFFFFF" FontWeight="SemiBold"/>
                        </StackPanel>
                    </DockPanel>
                </StackPanel>
            </Border>

            <!-- Chat driver -->
            <TextBlock Text="CHAT DRIVER" Foreground="#88FFFFFF"
                       FontSize="{DynamicResource AppFontSizeHint}"
                       FontWeight="SemiBold" Margin="0,0,0,4"/>
            <Border Background="#FF252525" CornerRadius="4" Padding="14,10" Margin="0,0,0,16">
                <StackPanel>
                    <DockPanel Margin="0,0,0,6">
                        <TextBlock DockPanel.Dock="Left" Text="Mode"
                                   Foreground="#CCFFFFFF" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Right"
                                   Text="{Binding ChatModeText}"
                                   Foreground="#FFFFFFFF" FontWeight="SemiBold"/>
                    </DockPanel>
                    <DockPanel Margin="0,0,0,6">
                        <TextBlock DockPanel.Dock="Left" Text="Frames processed"
                                   Foreground="#CCFFFFFF" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Right"
                                   Text="{Binding ChatFrames}"
                                   FontFamily="{DynamicResource AppMonoFontFamily}"
                                   Foreground="#FFFFFFFF"/>
                    </DockPanel>
                    <DockPanel Margin="0,0,0,6">
                        <TextBlock DockPanel.Dock="Left" Text="Last log timestamp (UTC)"
                                   Foreground="#88FFFFFF" VerticalAlignment="Center"/>
                        <TextBlock HorizontalAlignment="Right"
                                   Text="{Binding ChatLastLog}"
                                   FontFamily="{DynamicResource AppMonoFontFamily}"
                                   Foreground="#88FFFFFF"/>
                    </DockPanel>
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Left" Text="Tailer-poll age"
                                   Foreground="#CCFFFFFF" VerticalAlignment="Center"/>
                        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                            <Ellipse Width="8" Height="8" Fill="#FFE04545"
                                     VerticalAlignment="Center" Margin="0,0,6,0">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ChatMode}" Value="Stalled">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding ChatMode}" Value="Halted">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <TextBlock Text="{Binding ChatDrift}"
                                       FontFamily="{DynamicResource AppMonoFontFamily}"
                                       Foreground="#FFFFFFFF" FontWeight="SemiBold"/>
                        </StackPanel>
                    </DockPanel>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
