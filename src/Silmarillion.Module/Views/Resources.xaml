<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:wpf="clr-namespace:Mithril.Shared.Wpf;assembly=Mithril.Shared.Wpf"
                    xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks">
    <!-- Converters.xaml merged locally so {StaticResource NullOrEmptyToVis} inside the
         DataTemplates below resolves at template parse time. The outer UserControl's
         resource merge is too late for StaticResource lookups inside merged DataTemplates. -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Mithril.Shared.Wpf;component/Converters.xaml"/>
    </ResourceDictionary.MergedDictionaries>


    <!-- Compact two-line row for Items tab. Bound DataContext: Mithril.Reference.Models.Items.Item.
         Replaces the card layout in #231; ~36px per row, ~40+ visible. Key name kept as
         "*CardTemplate" to avoid rippling into both tab views — names are wallpaper. -->
    <DataTemplate x:Key="ItemCardTemplate">
        <DockPanel LastChildFill="True">
            <wpf:IconImage DockPanel.Dock="Left" IconId="{Binding IconId}"
                           Width="24" Height="24" Margin="6,3,6,3" VerticalAlignment="Center"/>
            <StackPanel VerticalAlignment="Center" Margin="0,3,6,3">
                <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                           Foreground="#FFD4A847" TextTrimming="CharacterEllipsis"
                           FontSize="{DynamicResource AppFontSizeHint}"/>
                <TextBlock Text="{Binding EquipSlot, Converter={StaticResource CamelCaseSplit}}"
                           Foreground="#88FFFFFF"
                           FontSize="{DynamicResource AppFontSizeSmall}"
                           TextTrimming="CharacterEllipsis"
                           Visibility="{Binding EquipSlot, Converter={StaticResource NullOrEmptyToVis}}"/>
            </StackPanel>
        </DockPanel>
    </DataTemplate>

    <!-- Compact two-line row for NPCs tab. Bound DataContext: Silmarillion.ViewModels.NpcListRow.
         Icon-less for now (NPC entries don't ship icon ids in npcs.json); a Users icon stands in
         to keep the row height consistent with the Items/Recipes tabs. -->
    <DataTemplate x:Key="NpcCardTemplate">
        <DockPanel LastChildFill="True">
            <Border DockPanel.Dock="Left" Width="24" Height="24" Margin="6,3,6,3"
                    VerticalAlignment="Center" Background="#FF1F1F1F" CornerRadius="3">
                <icon:PackIconLucide Kind="User" Width="14" Height="14" Foreground="#88FFFFFF"
                                     HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel VerticalAlignment="Center" Margin="0,3,6,3">
                <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                           Foreground="#FFD4A847" TextTrimming="CharacterEllipsis"
                           FontSize="{DynamicResource AppFontSizeHint}"/>
                <TextBlock Text="{Binding AreaDisplayName}" Foreground="#88FFFFFF"
                           FontSize="{DynamicResource AppFontSizeSmall}"
                           TextTrimming="CharacterEllipsis"
                           Visibility="{Binding AreaDisplayName, Converter={StaticResource NullOrEmptyToVis}}"/>
            </StackPanel>
        </DockPanel>
    </DataTemplate>

    <!-- Compact two-line row for Quests tab. Bound DataContext: Silmarillion.ViewModels.QuestListRow.
         Quest entries don't ship icon ids; a ScrollText icon stands in (matches the empty-state
         placeholder on the right pane). Secondary line shows Level + Location chip-style. -->
    <DataTemplate x:Key="QuestCardTemplate">
        <DockPanel LastChildFill="True">
            <Border DockPanel.Dock="Left" Width="24" Height="24" Margin="6,3,6,3"
                    VerticalAlignment="Center" Background="#FF1F1F1F" CornerRadius="3">
                <icon:PackIconLucide Kind="ScrollText" Width="14" Height="14" Foreground="#88FFFFFF"
                                     HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel VerticalAlignment="Center" Margin="0,3,6,3">
                <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                           Foreground="#FFD4A847" TextTrimming="CharacterEllipsis"
                           FontSize="{DynamicResource AppFontSizeHint}"/>
                <TextBlock Foreground="#88FFFFFF"
                           FontSize="{DynamicResource AppFontSizeSmall}"
                           TextTrimming="CharacterEllipsis">
                    <TextBlock.Text>
                        <PriorityBinding>
                            <Binding Path="DisplayedLocation"/>
                            <Binding Path="FavorNpcDisplayName"/>
                        </PriorityBinding>
                    </TextBlock.Text>
                </TextBlock>
            </StackPanel>
        </DockPanel>
    </DataTemplate>

    <!-- Compact two-line row for Abilities tab. Bound DataContext: Silmarillion.ViewModels.AbilityListRow.
         Icon comes from Ability.IconID (most abilities ship one); the row's secondary line shows
         Skill + Level + optional Rank. -->
    <DataTemplate x:Key="AbilityCardTemplate">
        <DockPanel LastChildFill="True">
            <wpf:IconImage DockPanel.Dock="Left" IconId="{Binding IconID}"
                           Width="24" Height="24" Margin="6,3,6,3" VerticalAlignment="Center"/>
            <StackPanel VerticalAlignment="Center" Margin="0,3,6,3">
                <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                           Foreground="#FFD4A847" TextTrimming="CharacterEllipsis"
                           FontSize="{DynamicResource AppFontSizeHint}"/>
                <TextBlock Foreground="#88FFFFFF"
                           FontSize="{DynamicResource AppFontSizeSmall}"
                           TextTrimming="CharacterEllipsis">
                    <Run Text="{Binding Skill, Mode=OneWay}"/>
                    <Run Text=" "/>
                    <Run Text="{Binding Level, Mode=OneWay}"/>
                </TextBlock>
            </StackPanel>
        </DockPanel>
    </DataTemplate>

    <!-- Compact two-line row for Recipes tab. Bound DataContext: Silmarillion.ViewModels.RecipeListRow. -->
    <DataTemplate x:Key="RecipeCardTemplate">
        <DockPanel LastChildFill="True">
            <wpf:IconImage DockPanel.Dock="Left" IconId="{Binding IconId}"
                           Width="24" Height="24" Margin="6,3,6,3" VerticalAlignment="Center"/>
            <StackPanel VerticalAlignment="Center" Margin="0,3,6,3">
                <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                           Foreground="#FFD4A847" TextTrimming="CharacterEllipsis"
                           FontSize="{DynamicResource AppFontSizeHint}"/>
                <TextBlock Foreground="#88FFFFFF"
                           FontSize="{DynamicResource AppFontSizeSmall}"
                           TextTrimming="CharacterEllipsis"
                           Visibility="{Binding SkillDisplayName, Converter={StaticResource NullOrEmptyToVis}}">
                    <Run Text="{Binding SkillDisplayName, Mode=OneWay}"/>
                    <Run Text=" "/>
                    <Run Text="{Binding SkillLevelReq, Mode=OneWay}"/>
                </TextBlock>
            </StackPanel>
        </DockPanel>
    </DataTemplate>

</ResourceDictionary>
