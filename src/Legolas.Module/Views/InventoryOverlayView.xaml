<Window x:Class="Legolas.Views.InventoryOverlayView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Legolas.Controls"
        xmlns:vm="clr-namespace:Legolas.ViewModels"
        Title="Inventory Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        Width="540" Height="440"
        d:DataContext="{d:DesignInstance vm:InventoryOverlayViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d">
    <Grid>
        <!-- Backdrop layer: opacity slider affects only this, not the slot tiles
             or header on top. See MapOverlayView for the same pattern. -->
        <Rectangle Fill="#80202020" IsHitTestVisible="False"
                   Opacity="{Binding Session.InventoryOpacity, FallbackValue=1.0}" />
        <Border BorderBrush="#FF404040" BorderThickness="1" Padding="6">
        <DockPanel>
            <Border x:Name="HeaderRegion" DockPanel.Dock="Top" Background="#FF303030" Padding="8,4" CornerRadius="2"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <TextBlock Text="Inventory" Foreground="White" FontWeight="SemiBold" />
            </Border>

            <ItemsControl ItemsSource="{Binding Slots}" Margin="0,6,0,0">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <controls:InventoryGridPanel
                            Columns="{Binding Grid.Columns}"
                            CellWidth="{Binding Grid.CellWidth}"
                            CellHeight="{Binding Grid.CellHeight}"
                            ColumnGap="{Binding Grid.ColumnGap}"
                            RowGap="{Binding Grid.RowGap}" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:SurveyItemViewModel}">
                        <!-- Tile: BorderBrush + child text stay opaque; the gray fill fades with the
                             inventory opacity slider via the inner Rectangle. Putting the fade on a
                             dedicated background rectangle (rather than the Border's Opacity) avoids
                             dimming the slot text and route-order numbers. -->
                        <Border BorderThickness="2" CornerRadius="2">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="BorderBrush">
                                        <Setter.Value>
                                            <SolidColorBrush Color="#FF606060" />
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsActiveTarget}" Value="True">
                                            <DataTrigger.EnterActions>
                                                <BeginStoryboard Name="PulseStoryboard">
                                                    <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                                        <ColorAnimation
                                                            Storyboard.TargetProperty="BorderBrush.Color"
                                                            From="#FF606060" To="#FFFFD700"
                                                            Duration="0:0:0.6" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.EnterActions>
                                            <DataTrigger.ExitActions>
                                                <StopStoryboard BeginStoryboardName="PulseStoryboard" />
                                            </DataTrigger.ExitActions>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Rectangle Fill="#FF404040" IsHitTestVisible="False"
                                           Opacity="{Binding DataContext.Session.InventoryOpacity, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=1.0}" />
                                <TextBlock Text="{Binding Name}" Foreground="White"
                                           TextWrapping="Wrap" TextTrimming="CharacterEllipsis"
                                           Margin="4" FontSize="{DynamicResource AppFontSizeSmall}" />
                                <TextBlock Text="{Binding GridIndex, Converter={x:Static controls:OneBasedConverter.Instance}}"
                                           Foreground="#FFAAAAAA"
                                           HorizontalAlignment="Left" VerticalAlignment="Bottom"
                                           Margin="4,0,0,2" FontSize="{DynamicResource AppFontSizeXSmall}" />
                                <TextBlock Text="{Binding RouteOrder, Converter={x:Static controls:OneBasedConverter.Instance}}"
                                           Foreground="Gold"
                                           HorizontalAlignment="Right" VerticalAlignment="Top"
                                           Margin="0,2,4,0" FontSize="{DynamicResource AppFontSizeSmall}" FontWeight="Bold"
                                           Visibility="{Binding RouteOrder, Converter={x:Static controls:NullToVisibilityConverter.Instance}}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </DockPanel>
        </Border>
        <controls:ResizeGrips x:Name="GripsRegion" />
    </Grid>
</Window>
